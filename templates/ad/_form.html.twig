<div class="container w-50">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="text-center">
            <button class="btn btn-primary w-75 m-auto">{{ button_label|default('Save') }}</button>
        </div>
    {{ form_end(form) }}
</div>
